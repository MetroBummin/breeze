# Roadmap

## 방향 전환 (2026-08)

원서 리더로 출발했지만 유통되는 EPUB은 사실상 전부 DRM이 걸려 있습니다. 앱을 정상적으로 쓰려면 사용자가 비정상적인 경로로 파일을 구해야 한다는 뜻이고, 그건 제품의 전제로 삼을 수 없습니다.

그래서 **읽을거리를 파일에 의존하지 않는 쪽**으로 무게중심을 옮깁니다. 원서를 버리는 것은 아닙니다. 이미 만들어 둔 PDF/EPUB 읽기와 원본 모드는 그대로 두고, 그 위에 **파일 없이도 시작할 수 있는 입구**를 얹습니다.

Breeze의 진짜 자산은 파일 포맷 지원이 아니라 **텍스트 → 단어 → 뜻 → 단어장으로 이어지는 매핑 엔진**입니다. 그 엔진은 글이 어디서 왔는지 신경 쓰지 않습니다.

## 지금 상태

| | 상태 |
|---|---|
| PDF·EPUB 읽기 (글자 / 원본 모드) | 있음 |
| 단어 탭 → 사전 → 단어장 → 기기 간 동기화 | 있음 |
| 내장 사전 300 표제어 + 문맥 AI 뜻 | 있음 |
| **영어 글 붙여넣기** | **있음** |
| **수능·모의고사 문항 분리 (규칙 기반)** | **있음** |
| **기출 쇼츠 모드 (타이머·잠금·사전)** | **있음** |
| 기출 채점 | 없음 (문제지에 정답표가 없음) |
| 링크 공유로 바로 열기 | 없음 |
| 유료 아티클 인앱 브라우저 오버레이 | 없음 |
| AI 쌍둥이 변형 문제 | 없음 |

## 1단계 — 파일 없는 입구 (일부 완료)

### 붙여넣기 (완료)

상단바의 붙여넣기 버튼 또는 홈의 `영어 글 붙여넣기` 카드로 아무 영어 글이나 바로 읽기 화면에 올립니다. 구조를 알려주는 메타데이터가 없으므로 규칙은 눈에 보이는 것만 씁니다.

```
첫 줄        -> 제목
빈 줄        -> 문단 경계
빈 줄이 없음 -> 줄마다 한 장의 카드 (X 스레드)
```

규칙이 세 줄이라 결과를 미리 예상할 수 있고, 예상할 수 있으면 버그로 보이지 않습니다. 하드 랩된 줄은 기존 `mergeWrapped`가 다시 이어 붙입니다. 저장은 다른 책과 같은 IndexedDB로 가므로 **넣는 순간부터 오프라인**입니다. 구현: `parsePastedText()` (`scripts/importers/importers.js`).

### 남은 것

- **무료 고전 5종 내장.** 프로젝트 구텐베르크 퍼블릭 도메인. 어떤 5종을 넣을지는 정해지지 않았습니다.
- **링크 공유.** 웹에서는 PWA `manifest.json`의 `share_target`으로 안드로이드 크롬 공유 시트에 등록할 수 있습니다. iOS 사파리는 `share_target`을 지원하지 않으므로, iOS까지 덮으려면 네이티브 셸(Capacitor 등)이 필요합니다.
- **URL → 본문 추출.** 정적 사이트에서 남의 도메인을 `fetch`하면 CORS에 막힙니다. 이미 쓰고 있는 Supabase Edge Function에 추출 엔드포인트를 하나 더 두는 것이 현실적인 경로입니다.

## 2단계 — 유료 아티클 돌파

인앱 브라우저(WebView)로 사용자가 직접 미디엄 등에 로그인하게 하고, 그 화면에 자바스크립트를 주입해 기존 사전 바텀시트를 결합합니다.

**웹앱으로는 불가능합니다.** 남의 출처 페이지에 스크립트를 주입하는 것은 브라우저 보안 모델이 막습니다. 이 단계는 네이티브 셸을 전제로 합니다. 대신 사전 패널과 단어장 로직은 이미 출처와 무관하게 동작하므로, 셸만 생기면 그대로 얹힙니다.

## 3단계 — 수능 기출 · 쇼츠 모드 (완료)

> **시험지 조판·문항 분리에는 AI를 쓰지 않습니다.** 아래는 실제 기출 10종으로 측정해 확인한 결과입니다.

### 측정 결과 (2026-08, 기출 10종)

대상: 2026 수능, 2022 대수능 9월 모평, 2025 고3 9월, 2022·2023·2026 고1/고2 3월·9월.
범위: **읽기 18~24, 29~45 (24문항)**. 듣기 1~17과 도표·안내문 25~28은 제외합니다.

| | 결과 |
|---|---|
| 목표 문항 검출 | **240 / 240 (10종 × 24)** |
| 발문 누락 | 0 |
| 지문 누락·과소 | 0 |
| 선택지 누락 | 0 |
| 좌우 단 접합 줄 | 1,931 → 26 |

규칙만으로 결함 0입니다. LLM 보정이 필요한 구간은 나오지 않았습니다.

### 확인된 규칙

1. **단 가르기** — 글자가 거의 지나가지 않는 세로 띠를 찾아 좌/우 단을 나눕니다. 페이지 폭 30~70% 구간에서 폭 6pt 이상, 띠를 실제로 넘는 글자가 전체의 2% 미만. 구현 완료: `detectColumnGutter()` / `pdfPageColumns()`.
2. **읽기 순서** — 왼쪽 단 위→아래, 그다음 오른쪽 단. 단 경계는 문단을 이어 붙이면 안 되므로 쪽 경계와 같게 다룹니다.
3. **머리글·꼬리글** — 여러 쪽에 반복되면서 위/아래 띠에 있는 짧은 줄만 버립니다. 문구 목록이 필요 없습니다.
4. **문항 시작** — 본문보다 왼쪽으로 내어쓴(outdent > 2pt) 줄이 `^\d{1,2}\.`로 시작할 때. 내어쓰기 조건이 없으면 본문 속 숫자에 걸립니다.
5. **묶음 문항** — `[31~34]` 형태. **물결표는 전각 `～`(U+FF5E)도 받아야 합니다** — 수능은 전각을 쓰고 모의고사는 반각을 씁니다. 이 한 글자 때문에 수능 3종의 31~34·36~39 발문과 41~45 지문이 통째로 비었습니다.
6. **공용 지문** — 묶음 머리글과 그 묶음의 첫 문항 번호 사이의 글이 41~42, 43~45의 공용 지문입니다.
7. **발문** — 문항 첫 줄이 한글이고 `?`나 `~시오`로 끝나면 그 문항의 발문, 없으면 묶음 지시문을 씁니다.
8. **선택지** — `①`로 **시작하는** 마지막 줄부터 끝까지. 어법·흐름 문제(29·35)처럼 ①~⑤가 지문 안에 박힌 경우는 줄 시작이 아니므로 지문이 잘리지 않습니다.

9. **빈칸 표시** — 빈칸 문제의 밑줄은 그림이라 글자로 안 뽑히고 자리만 비어 옵니다. 낱말 사이 공백은 0.6em, 빈칸 자리는 7.8em이었습니다(열두 배). 2.5em을 넘는 가로 빈틈만 `______`로 되살립니다. 선택지는 격자로 조판돼 칸 사이가 넓으므로 지문에만 적용합니다.

### 구현 상태

규칙 1은 반입기(`detectColumnGutter`), 4~9는 `scripts/importers/exam.js`, 화면은 `scripts/shorts/shorts.js`에 있습니다.

## 쇼츠 모드

- 세로 풀스크린 스냅. 한 화면에 한 문항, 위로 넘기면 다음 문항.
- 상단은 가운데 Breeze 로고만. 진행도 바 자리를 **타이머**가 씁니다 — 꽉 찬 상태에서 줄어들며 파랑(205°)에서 빨강(0°)으로 내려옵니다.
- 시간이 흐르는 동안 **단어 탭이 잠깁니다**. 답을 고르거나 시간이 끝나면 풀려서 사전 바텀시트가 열립니다.
- 문항별 제한 시간은 실제 시험 배점 시간에서 뽑았습니다(빈칸 100초, 장문 120초, 목적·심경 60초 …).
- 진입: 상단 nav의 `Shorts`, 또는 홈에서 좌우 스와이프. 하단 네비게이션 바는 만들지 않았습니다 — 최상위 화면이 세 개뿐이라 상시 크롬만 늘 뿐입니다.

### 아직 없는 것

- **채점.** 문제지 PDF에는 정답표가 없습니다. 고른 답을 표시만 하고 맞았다고도 틀렸다고도 말하지 않습니다. 정답표를 넣는 방법이 정해지면 그때 채점합니다.
- AI 쌍둥이 변형 문제, 해설.
- **저작권.** 사용자가 직접 올린 PDF를 분석하는 도구이므로 기출 원문을 배포하지 않습니다. 변형 문제를 만든다면 AI가 새로 창작한 것만 서비스가 생성합니다.

## 설계 원칙 (바뀌지 않는 것)

1. 원본 파일은 사용자 기기에만 둡니다. 서버에는 추출된 글자와 작은 좌표 지도만 갑니다.
2. 넣은 글은 넣는 순간부터 오프라인에서 읽힙니다.
3. 추측이 필요한 조판 판단은 하지 않습니다. 애매하면 평범한 본문입니다.
4. 빌드 도구 없이 정적 배포를 유지합니다.
