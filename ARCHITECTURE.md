# Architecture

## 원칙

1. 원본 PDF/EPUB Blob은 사용자의 기기에만 저장하고 서버 동기화 payload에 넣지 않습니다.
2. 추출 본문, 보수적인 블록 정보, 원본 위치 지도는 서로 분리합니다.
3. 목차 UI와 AI 전권 조판은 실행 경로에 포함하지 않습니다.
4. 빌드 도구 없이도 기존 정적 배포 방식을 유지합니다.
5. 파일 간 의존성은 `index.html`의 script 순서로 명시합니다.

## 파일 책임

- `scripts/core/`: 부팅 안전장치, IndexedDB, 상태
- `scripts/library/`: 홈 두 줄(Casuals·Long-form), 라이브러리 두 개, 추가 시트, 추가·삭제·이름 변경
  - `classics.js`: 내장 무료 고전 목록과 받기
- `scripts/importers/`: TXT/PDF/EPUB 파싱
  - `article.js`: 기사 URL — 본문 추출 규칙과 가져오기
- `scripts/dictionary/`: 표제어 처리, 사전 조회, 단어장 UI
- `scripts/reader/`: 보수적인 글자 화면, PDF/EPUB 원본 화면, 양방향 위치 연결
  - `pdf-word-geometry.js`: DOM에 의존하지 않는 순수 단어 상자 계산 (테스트가 직접 호출)
  - `mode-bridge.js`: 문장 단위 비교. 줄바꿈·따옴표·공백 차이를 무시합니다
  - `original-session.js`: 원본 세션 상태·수명·앵커·진행도
  - `pdf-original.js` / `epub-original.js`: 형식별 렌더링·단어 선택·앵커
  - `reader-modes.js`: `글자` ↔ `원본` 전환과 전환 표시
  - `reader.js`: 글자 화면 렌더링, 스크롤·폭 변화 대응
- `scripts/ui/`: 보기 설정, 발음, 시트 제스처
- `scripts/sync/`: 로그인, 단어·위치·책 동기화
- `server/dict/`: 활성 AI 사전 Edge Function
- `server/article/`: 기사 HTML·사진 중계 Edge Function (추출은 하지 않습니다)
- `assets/classics/`: 함께 배포하는 퍼블릭 도메인 EPUB 5종
- `modules/exam-shorts/`: **연결 해제된** 기출 문항 분리 + 쇼츠 화면.
  `index.html`이 읽지 않습니다. 되살리는 순서는 그 폴더의 `README.md`에 있고,
  `exam.js`의 규칙은 떼어 둔 동안에도 `npm test`가 계속 검사합니다.

## 홈

두 줄입니다. **Casuals**(`kind: paste | article`)는 가로 레일, **Long-form**(그
밖의 전부 + 아직 안 받은 내장 고전)은 격자. "이어서 읽기" 칸은 만들지
않았습니다 — 만들면 같은 책이 한 화면에 두 번 나옵니다. 대신 마지막에 열었던
것 하나에 테두리를 두르고 진행도 글귀 앞에 `이어서`를 붙입니다.

**두 줄은 각자 자기 줄의 마지막 것을 기억합니다**(`nowReadingIn(list)`).
지하철에서 기사를 한 편 봤다고 읽던 원서 표시가 사라지면 안 되기
때문입니다. 줄마다 라이브러리(`#v-casuals` · `#v-longform`)와 `+`가 하나씩
있고, 두 라이브러리는 같은 카드를 격자에만 다시 깝니다.

`+`가 여는 것은 그 줄에 넣을 수 있는 것뿐입니다. Casuals 는 붙여넣기·기사
URL 두 가지라 시트를 열고, Long-form 은 파일 하나뿐이라 시트를 건너뛰고 곧장
파일 고르기로 갑니다. 상단바의 `+`만 셋을 다 보여줍니다.

샘플 책은 없앴습니다. 첫 화면을 채우는 일은 무료 고전 5종이 더 잘합니다 —
샘플은 지울 수도 이름을 바꿀 수도 없는데, 고전은 진짜 내 책이 됩니다.

내장 고전은 별도 읽기 경로가 없습니다. 카드를 누르면 `assets/classics`의 EPUB을
`fetch`해서 `File`로 만들어 평범한 `importFile()`에 넘깁니다. 그래서 `원본`
모드, 단어장, 동기화가 전부 저절로 따라옵니다.

## 기사 URL

본문 추출은 **기기 안에서** 브라우저의 `DOMParser`로 합니다. 규칙만 씁니다.

1. `script·style·nav·header·footer·aside·form·table·figure` 따위를 먼저 지웁니다.
2. 본문 뿌리 = 50자 넘는 `<p>`가 가장 많이 모인 조상. 위로 갈수록 점수를 깎아
   페이지 전체가 이기지 않게 합니다. 사이트가 `<article>`을 붙여 두었고 그
   안에 본문이 들어 있으면 그쪽을 씁니다.
3. 클래스·id에 `comment·promo·related·reference` 같은 말이 있으면 버립니다.
   각주처럼 자기 자신은 깨끗한 경우가 있어 조상 네 칸까지 봅니다.
4. `References`·`External links` 같은 소제목을 만나면 거기서 멈춥니다.
5. 끝에 붙는 `Most viewed` 같은 것은 언제나 짧으므로, **마지막 진짜 문단**(100자
   이상) 뒤를 통째로 자릅니다.
6. 본문이 500자에 못 미치면 실패로 봅니다. 로그인·결제가 걸린 기사가 여기
   걸리며, 그때는 "못 가져왔다"고 말하고 붙여넣기를 권합니다.

### 사진

사진은 대부분 `<figure>` 안에 있고 그 `<figure>`는 규칙 1에서 통째로
버려집니다. 그래서 **버리기 전에** `articleMarkImages()`가 자리를 남기는 표시
하나로 바꿔 둡니다. 순서가 뒤집히면 사진이 전부 사라집니다.

- `srcset`이 있으면 **가장 큰 판**을 씁니다. 화면에 처음 뜨는 480px 판을
  그대로 담으면 카드 표지가 뭉개집니다.
- 크기는 **긴 변**으로 봅니다(200px 미만이면 아이콘). 두 변을 다 재면 250×144
  같은 가로 사진이 함께 걸러집니다. 짧은 변은 60px 미만만 막습니다(구분선).
- 걸러낼 때는 **그 `<img>` 하나만** 버립니다. 담고 있는 `<figure>`까지 지우면
  안 됩니다 — BBC는 사진과 매체 로고를 한 `<figure>`에 같이 넣어 두어서,
  로고를 버리다 사진이 통째로 날아갔습니다.
- 한 편에 최대 8장. 세는 것은 **살아남은 것만**입니다. 표시할 때 세면 표 안의
  배지처럼 곧 버려질 그림이 자릿수를 차지해 본문 사진이 밀려납니다.
- 대표 사진은 `og:image`, 없으면 본문 첫 사진. Casuals 카드의 표지가 됩니다.

받은 사진은 **넣는 순간 IndexedDB(`imgs`)에 담습니다.** 주소만 들고 있으면
비행기 안에서 빈 칸이 되고, 읽을 때마다 그 매체 서버에 발자국이 남습니다.
저장 키는 `art|<주소 해시>`입니다 — 책 ID는 그 키가 들어간 문단이 다 모여야
정해지므로, 키가 책 ID에 기댈 수 없습니다. 못 받은 사진은 조용히 빼고 문단을
다시 조립합니다. 사진 하나 때문에 기사를 통째로 못 읽으면 손해입니다.

기사에는 연결할 **원본 파일이 없습니다.** 원본 모드는 PDF·EPUB 전용이고,
기사에 그에 해당하는 것은 그 사이트의 살아 있는 페이지뿐이라 오프라인으로
가져올 수 없습니다. 대신 제목 아래에 원문으로 가는 링크(`#rsource`)를 둡니다.
영상·인터랙티브 도표도 같은 이유로 담지 않습니다.

서버(`server/article`)는 CORS만 넘겨 줍니다. `?url=`은 HTML을 그대로 돌려주고
파싱하지 않습니다 — 규칙을 고칠 때마다 함수를 다시 배포하고 싶지 않기
때문입니다. `&as=image`는 그림 바이트를 그대로 돌려줍니다. 열린 중계가 되지
않도록 사설 대역·비 HTTP 스킴을 막고 HTML은 3MB, 그림은 2MB에서 끊습니다.
SVG는 그림이 아니라 스크립트를 품을 수 있는 문서라 받지 않습니다.

## 호환성

IndexedDB `breeze-img`는 버전 3으로 올라가며 `originals` 저장소가 추가됩니다. 기존 `books`와 `imgs`, localStorage의 진행도·단어장 키는 유지합니다. 예전 `tidy`는 한 번 `formatting`으로 옮기고, `aiFormatting` 데이터는 읽지 않고 로컬 책에서 제거합니다.

## 읽기 모드

- `글자`: 반복 머리말 제거, 확실한 줄 연결, EPUB 의미 태그, 강한 크기·굵기 신호만 사용합니다. 애매하면 본문입니다.
- 2단 조판 PDF는 줄을 만들기 전에 단을 먼저 가릅니다. 글자가 거의 지나가지 않는 세로 띠를 찾는 순수 좌표 계산이며, 단이 없는 문서에서는 아무 일도 하지 않습니다. 단 경계는 쪽 경계와 같은 규칙으로 문단이 끊깁니다.
- `원본 PDF`: 페이지 placeholder를 먼저 만들고 현재 화면 앞뒤 페이지만 캔버스로 지연 렌더링합니다. 글자 좌표는 페이지 너비·높이 비율로 저장합니다.
- `원본 EPUB`: ZIP 자원을 로컬 Blob URL로 바꾸고, 스크립트·폼·프레임을 제거한 장을 sandbox iframe에 렌더링합니다.
- `sourceMap`: 문단마다 `{page,y}` 또는 `{href,spine,element}`를 저장합니다. `page`는 걸러낸 페이지를 뺀 순번이 아니라 **실제 PDF 페이지 번호**입니다. 전환할 때 가장 가까운 위치를 찾아 이동합니다.
- 진행도는 화면의 픽셀 비율이 아니라 PDF 페이지·EPUB 장/요소·글자 문단의 논리적 위치로 계산합니다. 전환 과정에서 앱이 만든 스크롤은 저장하지 않고, 사용자 스크롤만 약 800ms 단위로 기록합니다.
- 전환은 `sourceMap`보다 **눈에 보이는 문장**을 먼저 씁니다. 화면 맨 위 문장을 반대쪽 화면에서 찾아 그 자리로 이동하고, 못 찾을 때만 `sourceMap` 좌표로 갑니다.

## PDF 단어 상자

PDF에는 단어가 없고 위치가 찍힌 글자 조각만 있습니다. 조각을 한 줄로 주는 책도 있지만, **글자 하나씩** 주는 책도 많습니다(Verity가 그렇습니다).

```
"t" x=72   "h" x=76   "e" x=83.2   " " x=89.6   "t" x=93.4 …
```

조각마다 따로 단어를 뽑으면 글자 하나하나가 단어가 되어, 저장한 `h`가 페이지의 모든 `h`에 밑줄을 긋고 눌러도 낱글자만 찾습니다. 그래서 **같은 기준선의 조각을 한 줄로 되붙인 뒤에만** 단어를 나눕니다. 띄어쓰기는 조각 사이의 가로 간격으로 판단합니다(반입기의 줄 복원과 같은 규칙). 공백 조각이 알려주는 폭은 실제 간격과 다를 수 있어 쓰지 않습니다.

단어 상자는 페이지 크기 대비 비율로 저장하므로, 사전 패널이 열려 페이지 폭이 바뀌어도 다시 계산하지 않습니다. 클릭 판정과 하이라이트가 같은 상자를 씁니다. EPUB은 사용자가 더블클릭하거나 드래그한 네이티브 선택 범위를 읽습니다. 두 방식 모두 원본 글자층을 변형하지 않습니다.

## 보던 자리 지키기

폭이 바뀌면 글자 화면은 글이 다시 흐르고 원본 페이지는 비율대로 높이가 줄어듭니다. `#readmain`의 `ResizeObserver`가 사전 패널 개폐·창 크기 변경을 받아 방금 보던 앵커로 되돌립니다. 되돌리는 동안에는 앱이 만든 스크롤을 새 위치로 착각하지 않도록 앵커 기록을 잠급니다(`holdReaderAnchor`). 지연 렌더링된 PDF 페이지가 뒤늦게 실제 비율을 알게 되어 높이가 바뀔 때도, 그 페이지가 화면 위쪽에 있으면 같은 양만큼 스크롤을 옮겨 보이는 줄을 고정합니다.

## 로컬 원본 수명

앱은 가능한 경우 영구 저장 권한을 요청하지만 브라우저 정책이나 사용자가 사이트 데이터를 지우면 원본 Blob이 사라질 수 있습니다. 추출 본문과 서버 진행도가 남아 있으면 원본 모드에서 같은 파일을 다시 연결해 복구합니다.

서버 tombstone은 그보다 나중에 다시 반입한 로컬 원본을 지우지 않으며, 현재 열어 읽는 책에는 즉시 적용하지 않습니다. 예전 ID로 남은 원본은 파일 해시로 찾아 현재 책 ID에 다시 연결합니다.

## 동기화 안전장치

- 동시에 들어온 자동·수동 동기화 요청은 버리지 않고 현재 작업 뒤에 한 번 더 실행합니다.
- 책 삭제는 서버 목록에 tombstone을 먼저 기록한 뒤 Storage 본문과 위치를 지웁니다. 중간에 실패하면 다음 동기화에서 다시 시도합니다.
- 서버에서 받은 읽기 위치는 현재 열어 읽는 책의 화면을 갑자기 덮어쓰지 않습니다.
