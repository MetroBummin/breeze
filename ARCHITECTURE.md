# Architecture

## 원칙

1. 원문 문단 배열은 변경하지 않습니다.
2. 포맷 정보는 원문과 별도 객체로 저장합니다.
3. 목차 UI는 실행 경로에 포함하지 않습니다. 책 전체 디자인은 기기에서 분석하고, AI에는 구조 후보만 보냅니다.
4. 빌드 도구 없이도 기존 정적 배포 방식을 유지합니다.
5. 파일 간 의존성은 `index.html`의 script 순서로 명시합니다.

## 파일 책임

- `scripts/core/`: 부팅 안전장치, IndexedDB, 상태, 샘플 데이터
- `scripts/library/`: 책 목록, 추가·삭제·이름 변경
- `scripts/importers/`: TXT/PDF/EPUB 파싱
- `scripts/dictionary/`: 표제어 처리, 사전 조회, 단어장 UI
- `scripts/reader/`: 원문 보존 포맷 지도와 읽기 화면
- `scripts/ui/`: 보기 설정, 발음, 시트 제스처
- `scripts/sync/`: 로그인, 단어·위치·책 동기화
- `server/dict/`: 활성 AI 사전 Edge Function
- `server/format/`: 로그인 사용자의 제목·목차·인용문 후보만 위치 지도로 판정하는 Edge Function
- `legacy/`: 실행되지 않는 목차·전권 AI 정리 코드

## 호환성

이 버전은 기존 IndexedDB와 localStorage 키를 유지합니다. 예전 책의 `tidy.blocks`는 읽기 화면에서만 호환되며 새 책은 `formatting.blocks`를 사용합니다. AI 결과는 `aiFormatting.windows`에 원문과 분리해 저장합니다.

## 선택형 전권 AI 조판

- PDF/EPUB 반입 직후 위치·글꼴·들여쓰기 단서로 안정적인 기본판을 먼저 만듭니다.
- 일반 본문은 기본 역할로 확정하고 제목·목차·인용문처럼 구조가 의심되는 후보만 고릅니다.
- 한 권에서 AI로 보내는 후보는 최대 420개·약 55,000자로 제한합니다.
- 요청은 최대 64개·약 12,000자씩 나누되 모든 묶음에 같은 전권 디자인 문법을 제공합니다.
- AI는 본문을 생략하고 `h1/h2/h3/quote/note/toc` 예외의 `{i,n,r,b}` 위치 지도만 반환합니다.
- 원문 문자열과 설명은 출력하지 않습니다. 넓은 자간 복원도 PDF 좌표로 기기에서 수행합니다.
- 긴 다문장 제목, 과도한 인용문, 겹치는 범위는 서버와 클라이언트에서 이중으로 거부합니다.
- 모든 묶음이 검증될 때까지 기본판을 유지하고, 완성 지도 버전 3은 다음 책 열기에서 한 번에 적용합니다.
- 실패·로그아웃·비동의 상태에서도 기본판으로 즉시 읽을 수 있습니다.
